import colors from './colors';

const palette = {
  heading: {
    color: colors.title,
    fontSize: 20,
    textAlign: 'center',
  },
  text: {
    color: colors.text,
    fontSize: 17,
    textAlign: 'center',
  },
};

export default palette;
